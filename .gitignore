# Monorepo root ignores.
# The Rails app has its own rules in backend/.gitignore, and the React app in
# frontend/.gitignore. This file only covers things that live at the very top.
.DS_Store
.ruby-lsp/
